services:
  db:
    build: .
    command: ["postgres", "-c", "log_statement=all"]
    ports:
      - "5432:5432"
    volumes:
      - hrm_postgres_volume:/var/lib/postgresql
volumes:
  hrm_postgres_volume: { }